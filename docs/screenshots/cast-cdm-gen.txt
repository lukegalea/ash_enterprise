$ devenv shell -- mix cdm.gen.resource Account --domain Sales --ownership user_owned --dry-run
defmodule AshEnterprise.Sales.Account do
  @moduledoc """
  Generated by `mix cdm.gen.resource` from `priv/cdm/resolved/account.json`.

  Source: Account (microsoft/CDM, Account.cdm.json/Account).

  This is a starting point, not a finished resource. Per
  `docs/manifesto/02-schema-commons.md`, delete the columns you do not need
  and add the actions, calculations and relationships that are genuinely
  yours before this ships.
  """

  use AshEnterprise.Platform.Resource,
    domain: AshEnterprise.Sales,
    ownership: :user_owned,
    tenant?: true,
    cdm_entity: "Account"

  postgres do
    table "accounts"
    repo AshEnterprise.Repo
  end

  attributes do
    uuid_primary_key :id

  attribute :account_category_code, :integer do
    public? true
    description "Select a category to indicate whether the customer account is standard or preferred."
  end


  attribute :customer_size_code, :integer do
    public? true
    description "Select the size category or range of the account for segmentation and reporting purposes."
  end


  attribute :preferred_contact_method_code, :integer do
    public? true
    description "Select the preferred method of contact."
  end


  attribute :customer_type_code, :integer do
    public? true
    description "Select the category that best describes the relationship between the account and your organization."
  end


  attribute :account_rating_code, :integer do
    public? true
    description "Select a rating to indicate the value of the customer account."
  end


  attribute :industry_code, :integer do
    public? true
    description "Select the account's primary industry for use in marketing segmentation and demographic analysis."
  end


  attribute :territory_code, :integer do
    public? true
    description "Select a region or territory for the account for use in segmentation and analysis."
  end


  attribute :account_classification_code, :integer do
    public? true
    description "Select a classification code to indicate the potential value of the customer account based on the projected return on investment, cooperation level, sales cycle length or other criteria."
  end


  attribute :business_type_code, :integer do
    public? true
    description "Select the legal designation or other business type of the account for contracts or reporting purposes."
  end


  attribute :payment_terms_code, :integer do
    public? true
    description "Select the payment terms to indicate when the customer needs to pay the total amount."
  end


  attribute :shipping_method_code, :integer do
    public? true
    description "Select a shipping method for deliveries sent to the account's address to designate the preferred carrier or other delivery option."
  end


  attribute :primary_contact_id, :uuid do
    public? true
    description "Choose the primary contact for the account to provide quick access to contact details."
  end


  attribute :participates_in_workflow, :boolean do
    public? true
    description "For system use only. Legacy Microsoft Dynamics CRM 3.0 workflow data."
  end


  attribute :name, :string do
    allow_nil? false
    public? true
    constraints max_length: 160
    description "Type the company or business name."
  end


  attribute :account_number, :string do
    public? true
    constraints max_length: 20
    description "Type an ID number or code for the account to quickly search and identify the account in system views."
  end


  attribute :revenue, :decimal do
    public? true
    description "Type the annual revenue for the account, used as an indicator in financial performance analysis."
  end


  attribute :number_of_employees, :integer do
    public? true
    description "Type the number of employees that work at the account for use in marketing segmentation and demographic analysis."
  end


  attribute :description, :string do
    public? true
    constraints max_length: 2000
    description "Type additional information to describe the account, such as an excerpt from the company's website."
  end


  attribute :sic, :string do
    public? true
    constraints max_length: 20
    description "Type the Standard Industrial Classification (SIC) code that indicates the account's primary industry of business, for use in marketing segmentation and demographic analysis."
  end


  attribute :ownership_code, :integer do
    public? true
    description "Select the account's ownership structure, such as public or private."
  end


  attribute :market_cap, :decimal do
    public? true
    description "Type the market capitalization of the account to identify the company's equity, used as an indicator in financial performance analysis."
  end


  attribute :shares_outstanding, :integer do
    public? true
    description "Type the number of shares available to the public for the account. This number is used as an indicator in financial performance analysis."
  end


  attribute :ticker_symbol, :string do
    public? true
    constraints max_length: 10
    description "Type the stock exchange symbol for the account to track financial performance of the company. You can click the code entered in this field to access the latest trading information from MSN Money."
  end


  attribute :stock_exchange, :string do
    public? true
    constraints max_length: 20
    description "Type the stock exchange at which the account is listed to track their stock and financial performance of the company."
  end


  attribute :web_site_url, :string do
    public? true
    constraints max_length: 200
    description "Type the account's website URL to get quick details about the company profile."
  end


  attribute :ftp_site_url, :string do
    public? true
    constraints max_length: 200
    description "Type the URL for the account's FTP site to enable users to access data and share documents."
  end


  attribute :e_mail_address1, :string do
    public? true
    constraints max_length: 100
    description "Type the primary email address for the account."
  end


  attribute :e_mail_address2, :string do
    public? true
    constraints max_length: 100
    description "Type the secondary email address for the account."
  end


  attribute :e_mail_address3, :string do
    public? true
    constraints max_length: 100
    description "Type an alternate email address for the account."
  end


  attribute :do_not_phone, :boolean do
    public? true
    description "Select whether the account allows phone calls. If Do Not Allow is selected, the account will be excluded from phone call activities distributed in marketing campaigns."
  end


  attribute :do_not_fax, :boolean do
    public? true
    description "Select whether the account allows faxes. If Do Not Allow is selected, the account will be excluded from fax activities distributed in marketing campaigns."
  end


  attribute :telephone1, :string do
    public? true
    constraints max_length: 50
    description "Type the main phone number for this account."
  end


  attribute :do_not_e_mail, :boolean do
    public? true
    description "Select whether the account allows direct email sent from Microsoft Dynamics 365."
  end


  attribute :telephone2, :string do
    public? true
    constraints max_length: 50
    description "Type a second phone number for this account."
  end


  attribute :fax, :string do
    public? true
    constraints max_length: 50
    description "Type the fax number for the account."
  end


  attribute :telephone3, :string do
    public? true
    constraints max_length: 50
    description "Type a third phone number for this account."
  end


  attribute :do_not_postal_mail, :boolean do
    public? true
    description "Select whether the account allows direct mail. If Do Not Allow is selected, the account will be excluded from letter activities distributed in marketing campaigns."
  end


  attribute :do_not_bulk_e_mail, :boolean do
    public? true
    description "Select whether the account allows bulk email sent through campaigns. If Do Not Allow is selected, the account can be added to marketing lists, but is excluded from email."
  end


  attribute :do_not_bulk_postal_mail, :boolean do
    public? true
    description "Select whether the account allows bulk postal mail sent through marketing campaigns or quick campaigns. If Do Not Allow is selected, the account can be added to marketing lists, but will be excluded from the postal mail."
  end


  attribute :credit_limit, :decimal do
    public? true
    description "Type the credit limit of the account. This is a useful reference when you address invoice and accounting issues with the customer."
  end


  attribute :credit_on_hold, :boolean do
    public? true
    description "Select whether the credit for the account is on hold. This is a useful reference while addressing the invoice and accounting issues with the customer."
  end


  attribute :parent_account_id, :uuid do
    public? true
    description "Choose the parent account associated with this account to show parent and child businesses in reporting and analytics."
  end


  attribute :aging30, :decimal do
    public? true
    description "For system use only."
  end


  attribute :aging60, :decimal do
    public? true
    description "For system use only."
  end


  attribute :aging90, :decimal do
    public? true
    description "For system use only."
  end


  attribute :address1_address_id, :uuid do
    public? true
    description "Unique identifier for address 1."
  end


  attribute :address1_address_type_code, :integer do
    public? true
    description "Select the address type, such as primary or billing."
  end


  attribute :address1_name, :string do
    allow_nil? false
    public? true
    constraints max_length: 200
    description "Type a descriptive name for the customer's address, such as Corporate Headquarters."
  end


  attribute :address1_primary_contact_name, :string do
    public? true
    constraints max_length: 150
    description "Type the name of the primary contact person for the customer's address."
  end


  attribute :address1_line1, :string do
    allow_nil? false
    public? true
    constraints max_length: 250
    description "Type the first line of the customer's address to help identify the location."
  end


  attribute :address1_line2, :string do
    public? true
    constraints max_length: 250
    description "Type the second line of the customer's address."
  end


  attribute :address1_line3, :string do
    public? true
    constraints max_length: 250
    description "Type the third line of the customer's address."
  end


  attribute :address1_city, :string do
    public? true
    constraints max_length: 80
    description "Type the city for the customer's address to help identify the location."
  end


  attribute :address1_state_or_province, :string do
    public? true
    constraints max_length: 50
    description "Type the state or province of the customer's address."
  end


  attribute :address1_county, :string do
    public? true
    constraints max_length: 50
    description "Type the county for the customer's address."
  end


  attribute :address1_country, :string do
    public? true
    constraints max_length: 80
    description "Type the country or region for the customer's address."
  end


  attribute :address1_post_office_box, :string do
    public? true
    constraints max_length: 20
    description "Type the post office box number of the customer's address."
  end


  attribute :address1_postal_code, :string do
    public? true
    constraints max_length: 20
    description "Type the ZIP Code or postal code for the address."
  end


  attribute :address1_utc_offset, :string do
    public? true
    description "Select the time zone for the address."
  end


  attribute :address1_freight_terms_code, :integer do
    public? true
    description "Select the freight terms to make sure shipping charges are processed correctly."
  end


  attribute :address1_ups_zone, :string do
    public? true
    constraints max_length: 4
    description "Type the UPS zone of the customer's address to make sure shipping charges are calculated correctly and deliveries are made promptly, if shipped by UPS."
  end


  attribute :address1_latitude, :float do
    public? true
    description "Type the latitude value for the customer's address, for use in mapping and other applications."
  end


  attribute :address1_telephone1, :string do
    public? true
    constraints max_length: 50
    description "Type the primary phone number for the customer's address."
  end


  attribute :address1_longitude, :float do
    public? true
    description "Type the longitude value for the customer's address, for use in mapping and other applications."
  end


  attribute :address1_shipping_method_code, :integer do
    public? true
    description "Select a shipping method for deliveries sent to this address."
  end


  attribute :address1_telephone2, :string do
    public? true
    constraints max_length: 50
    description "Type a second phone number for the customer's address."
  end


  attribute :address1_telephone3, :string do
    public? true
    constraints max_length: 50
    description "Type a third phone number for the customer's address."
  end


  attribute :address1_fax, :string do
    public? true
    constraints max_length: 50
    description "Type the fax number associated with the customer's address."
  end


  attribute :address1_composite, :string do
    public? true
    constraints max_length: 1000
    description "Shows the complete address."
  end


  attribute :address2_address_id, :uuid do
    public? true
    description "Unique identifier for address 1."
  end


  attribute :address2_address_type_code, :integer do
    public? true
    description "Select the address type, such as primary or billing."
  end


  attribute :address2_name, :string do
    allow_nil? false
    public? true
    constraints max_length: 200
    description "Type a descriptive name for the customer's address, such as Corporate Headquarters."
  end


  attribute :address2_primary_contact_name, :string do
    public? true
    constraints max_length: 150
    description "Type the name of the primary contact person for the customer's address."
  end


  attribute :address2_line1, :string do
    allow_nil? false
    public? true
    constraints max_length: 250
    description "Type the first line of the customer's address to help identify the location."
  end


  attribute :address2_line2, :string do
    public? true
    constraints max_length: 250
    description "Type the second line of the customer's address."
  end


  attribute :address2_line3, :string do
    public? true
    constraints max_length: 250
    description "Type the third line of the customer's address."
  end


  attribute :address2_city, :string do
    public? true
    constraints max_length: 80
    description "Type the city for the customer's address to help identify the location."
  end


  attribute :address2_state_or_province, :string do
    public? true
    constraints max_length: 50
    description "Type the state or province of the customer's address."
  end


  attribute :address2_county, :string do
    public? true
    constraints max_length: 50
    description "Type the county for the customer's address."
  end


  attribute :address2_country, :string do
    public? true
    constraints max_length: 80
    description "Type the country or region for the customer's address."
  end


  attribute :address2_post_office_box, :string do
    public? true
    constraints max_length: 20
    description "Type the post office box number of the customer's address."
  end


  attribute :address2_postal_code, :string do
    public? true
    constraints max_length: 20
    description "Type the ZIP Code or postal code for the address."
  end


  attribute :address2_utc_offset, :string do
    public? true
    description "Select the time zone for the address."
  end


  attribute :address2_freight_terms_code, :integer do
    public? true
    description "Select the freight terms to make sure shipping charges are processed correctly."
  end


  attribute :address2_ups_zone, :string do
    public? true
    constraints max_length: 4
    description "Type the UPS zone of the customer's address to make sure shipping charges are calculated correctly and deliveries are made promptly, if shipped by UPS."
  end


  attribute :address2_latitude, :float do
    public? true
    description "Type the latitude value for the customer's address, for use in mapping and other applications."
  end


  attribute :address2_telephone1, :string do
    public? true
    constraints max_length: 50
    description "Type the primary phone number for the customer's address."
  end


  attribute :address2_longitude, :float do
    public? true
    description "Type the longitude value for the customer's address, for use in mapping and other applications."
  end


  attribute :address2_shipping_method_code, :integer do
    public? true
    description "Select a shipping method for deliveries sent to this address."
  end


  attribute :address2_telephone2, :string do
    public? true
    constraints max_length: 50
    description "Type a second phone number for the customer's address."
  end


  attribute :address2_telephone3, :string do
    public? true
    constraints max_length: 50
    description "Type a third phone number for the customer's address."
  end


  attribute :address2_fax, :string do
    public? true
    constraints max_length: 50
    description "Type the fax number associated with the customer's address."
  end


  attribute :address2_composite, :string do
    public? true
    constraints max_length: 1000
    description "Shows the complete address."
  end


  attribute :preferred_appointment_day_code, :integer do
    public? true
    description "Select the preferred day of the week for service appointments."
  end


  attribute :preferred_system_user_id, :uuid do
    public? true
    description "Choose the preferred service representative for reference when you schedule service activities for the account."
  end


  attribute :preferred_appointment_time_code, :integer do
    public? true
    description "Select the preferred time of day for service appointments."
  end


  attribute :merged, :boolean do
    public? true
    description "Shows whether the account has been merged with another account."
  end


  attribute :do_not_send_mm, :boolean do
    public? true
    description "Select whether the account accepts marketing materials, such as brochures or catalogs."
  end


  attribute :master_id, :uuid do
    public? true
    description "Shows the master account that the account was merged with."
  end


  attribute :last_used_in_campaign, :utc_datetime_usec do
    public? true
    description "Shows the date when the account was last included in a marketing campaign or quick campaign."
  end


  attribute :exchange_rate, :decimal do
    public? true
    description "Shows the conversion rate of the record's currency. The exchange rate is used to convert all money fields in the record from the local currency to the system's default currency."
  end


  attribute :transaction_currency_id, :uuid do
    public? true
    description "Choose the local currency for the record to make sure budgets are reported in the correct currency."
  end


  attribute :credit_limit_base, :decimal do
    public? true
    description "Shows the credit limit converted to the system's default base currency for reporting purposes."
  end


  attribute :aging30_base, :decimal do
    public? true
    description "The base currency equivalent of the aging 30 field."
  end


  attribute :revenue_base, :decimal do
    public? true
    description "Shows the annual revenue converted to the system's default base currency. The calculations use the exchange rate specified in the Currencies area."
  end


  attribute :aging90_base, :decimal do
    public? true
    description "The base currency equivalent of the aging 90 field."
  end


  attribute :market_cap_base, :decimal do
    public? true
    description "Shows the market capitalization converted to the system's default base currency."
  end


  attribute :aging60_base, :decimal do
    public? true
    description "The base currency equivalent of the aging 60 field."
  end


  attribute :yomi_name, :string do
    public? true
    constraints max_length: 160
    description "Type the phonetic spelling of the company name, if specified in Japanese, to make sure the name is pronounced correctly in phone calls and other communications."
  end


  attribute :time_spent_by_me_on_email_and_meetings, :string do
    public? true
    constraints max_length: 1250
    description "Total time spent for emails (read and write) and meetings by me in relation to account record."
  end


  attribute :created_by_external_party, :uuid do
    public? true
    description "Shows the external party who created the record."
  end


  attribute :modified_by_external_party, :uuid do
    public? true
    description "Shows the external party who modified the record."
  end


  attribute :primary_satori_id, :string do
    public? true
    constraints max_length: 200
    description "Primary Satori ID for Account"
  end


  attribute :primary_twitter_id, :string do
    public? true
    constraints max_length: 128
    description "Primary Twitter ID for Account"
  end


  attribute :sla_id, :uuid do
    public? true
    description "Choose the service level agreement (SLA) that you want to apply to the Account record."
  end


  attribute :sla_invoked_id, :uuid do
    public? true
    description "Last SLA that was applied to this case. This field is for internal use only."
  end


  attribute :on_hold_time, :integer do
    public? true
    description "Shows how long, in minutes, that the record was on hold."
  end


  attribute :last_on_hold_time, :utc_datetime_usec do
    public? true
    description "Contains the date and time stamp of the last on hold time."
  end


  attribute :follow_email, :boolean do
    public? true
    description "Information about whether to allow following email activity like opens, attachment views and link clicks for emails sent to the account."
  end


  attribute :marketing_only, :boolean do
    public? true
    description "Whether is only for marketing"
  end

  end

  actions do
    defaults [:read, :destroy]
    default_accept [:account_category_code, :customer_size_code, :preferred_contact_method_code, :customer_type_code, :account_rating_code, :industry_code, :territory_code, :account_classification_code, :business_type_code, :payment_terms_code, :shipping_method_code, :primary_contact_id, :participates_in_workflow, :name, :account_number, :revenue, :number_of_employees, :description, :sic, :ownership_code, :market_cap, :shares_outstanding, :ticker_symbol, :stock_exchange, :web_site_url, :ftp_site_url, :e_mail_address1, :e_mail_address2, :e_mail_address3, :do_not_phone, :do_not_fax, :telephone1, :do_not_e_mail, :telephone2, :fax, :telephone3, :do_not_postal_mail, :do_not_bulk_e_mail, :do_not_bulk_postal_mail, :credit_limit, :credit_on_hold, :parent_account_id, :aging30, :aging60, :aging90, :address1_address_id, :address1_address_type_code, :address1_name, :address1_primary_contact_name, :address1_line1, :address1_line2, :address1_line3, :address1_city, :address1_state_or_province, :address1_county, :address1_country, :address1_post_office_box, :address1_postal_code, :address1_utc_offset, :address1_freight_terms_code, :address1_ups_zone, :address1_latitude, :address1_telephone1, :address1_longitude, :address1_shipping_method_code, :address1_telephone2, :address1_telephone3, :address1_fax, :address1_composite, :address2_address_id, :address2_address_type_code, :address2_name, :address2_primary_contact_name, :address2_line1, :address2_line2, :address2_line3, :address2_city, :address2_state_or_province, :address2_county, :address2_country, :address2_post_office_box, :address2_postal_code, :address2_utc_offset, :address2_freight_terms_code, :address2_ups_zone, :address2_latitude, :address2_telephone1, :address2_longitude, :address2_shipping_method_code, :address2_telephone2, :address2_telephone3, :address2_fax, :address2_composite, :preferred_appointment_day_code, :preferred_system_user_id, :preferred_appointment_time_code, :merged, :do_not_send_mm, :master_id, :last_used_in_campaign, :exchange_rate, :transaction_currency_id, :credit_limit_base, :aging30_base, :revenue_base, :aging90_base, :market_cap_base, :aging60_base, :yomi_name, :time_spent_by_me_on_email_and_meetings, :created_by_external_party, :modified_by_external_party, :primary_satori_id, :primary_twitter_id, :sla_id, :sla_invoked_id, :on_hold_time, :last_on_hold_time, :follow_email, :marketing_only]

    create :create do
      primary? true
    end

    update :update do
      primary? true
    end
  end

  code_interface do
    define :create
    define :read
    define :update
    define :destroy
  end
end

